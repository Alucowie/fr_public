[constants]
arch = 'i686-w64-mingw32'

[binaries]
exe_wrapper = 'wine'
c = arch + '-gcc'
cpp = arch + '-g++'
ar = arch + '-ar'
strip = arch + '-strip'
windres = arch + '-windres'

[host_machine]
system = 'windows'
cpu_family = 'x86'
cpu = 'i686'
endian = 'little'
